[package]
name = "cuda-metrics"
version = "0.1.0"
edition = "2021"
description = "Telemetry and metrics — counters, gauges, histograms, timers, and health dashboards for agent observability"

[dependencies]
serde = { version = "1", features = ["derive"] }
